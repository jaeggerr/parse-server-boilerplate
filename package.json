{
  "name": "parse-server-boilerplate",
  "version": "0.1.0",
  "description": "Template for Parse Server with PostgreSQL running on Docker and File System adapter",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc --p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^8.0.19",
    "@types/parse": "^2.4.0",
    "eslint-plugin-typescript": "^0.3.0",
    "tslint": "^5.5.0",
    "tslint-config-standard": "^6.0.1",
    "typescript": "^2.4.2"
  },
  "author": "Cedric Kemp",
  "license": "ISC",
  "dependencies": {
    "parse": "^1.10.0"
  }
}
